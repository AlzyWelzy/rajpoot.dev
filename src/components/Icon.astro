---
import { renderIcon, type IconName } from "@/lib/icons";

type Props = { name: IconName; class?: string; dataIcon?: string };

const { name, class: className, dataIcon } = Astro.props;

const markup = renderIcon(name, { class: className, dataIcon });
---

<Fragment set:html={markup} />
